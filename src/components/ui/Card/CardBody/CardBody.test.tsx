import { render } from '@testing-library/react';
import CardBody from './CardBody';

describe('CardBody', () => {
    it('should render', () => {
        render(<CardBody />);

        // TODO: Implement tests
    });
});
