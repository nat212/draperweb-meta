import { render } from '@testing-library/react';
import Card from './Card';

describe('Card', () => {
    it('should render', () => {
        render(<Card />);

        // TODO: Implement tests
    });
});
