import { render } from '@testing-library/react'
import CardTitle from './CardTitle'

describe('CardTitle', () => {
    it('should render', () => {
        render(<CardTitle />)

        // TODO: Implement tests
    })
})
